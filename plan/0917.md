express

site-scrapper.js

npm install request-promise

const rp = req

export defualt 
async function test(url) {
    const result = await rp(url);

}
rp(rul);

npm cors

npm install cors --save



---------------

rp를 만든다.